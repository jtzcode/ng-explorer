import { CraftManDirective } from './craft-man.directive';

describe('CraftManDirective', () => {
  it('should create an instance', () => {
    const directive = new CraftManDirective();
    expect(directive).toBeTruthy();
  });
});
